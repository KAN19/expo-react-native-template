import { View, Text } from "react-native";
import React from "react";

type Props = {};

const EndCallScreen = (props: Props) => {
	return (
		<View>
			<Text>EndCallScreen</Text>
		</View>
	);
};

export default EndCallScreen;
